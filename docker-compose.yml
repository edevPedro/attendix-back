services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: attendix
      POSTGRES_PASSWORD: attendix
      POSTGRES_DB: attendix
    ports:
      - "5432:5432"
    volumes:
      - attendix_pg:/var/lib/postgresql/data

volumes:
  attendix_pg:
